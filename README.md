# 8 Hour Timer ⏱️
A sleek Arabic 8-hour countdown timer built in pure HTML, CSS, and JavaScript.

## 🚀 Run with Docker
```bash
docker build -t 8h-timer .
docker run -d -p 8080:80 8h-timer
```
Then open: [http://localhost:8080](http://localhost:8080)

## 🧩 Docker Compose
```bash
docker-compose up -d
```

---
© 2025 — Built by Osaid 🛠️
